#ifndef common_h
#define common_h

//#define AMK_ASSERT(x) assert(x)
#define AMK_ASSERT(x)

#define _max(a, b) (( (a) > (b) ) ? (a) : (b))
#define _min(a, b) (( (a) < (b) ) ? (a) : (b))

#endif // common_h
